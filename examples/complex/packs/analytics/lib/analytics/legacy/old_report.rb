# frozen_string_literal: true

# Legacy files are ignored by the autoloader (not autoloaded).
# They can still be explicitly required when needed.
module Analytics
  module Legacy
    class OldReport
    end
  end
end
